# Frontend Environment Variables

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Protocol Buffer Configuration
# Set to 'true' to use Protocol Buffers for API communication
# Set to 'false' to use JSON (default REST behavior)
NEXT_PUBLIC_USE_PROTOBUF=true

# Note: When NEXT_PUBLIC_USE_PROTOBUF is enabled:
# - API requests will use 'application/x-protobuf' Content-Type
# - API responses will be requested with 'Accept: application/x-protobuf'
# - Data will be binary encoded/decoded automatically
# - Better performance and smaller payload sizes
# 
# When disabled:
# - Standard JSON REST API behavior
# - Human-readable requests/responses
# - Compatible with all tools (Postman, curl, etc.)
